export interface fund {
  receipt_no: string;
  name: string;
  building: string;
  mode_of_payment: string;
  date: string;
  place: string;
  marked_as_pay_later: string;
  amount: number;
  year: string;
}
